import { Outlet } from '@remix-run/react'

// meta

// links

// actions

// loaders

// page component
export default function ExpensesLayout() {
  return (
    <div>
      <h1>Base Layout</h1>
      <Outlet />
    </div>
  )
}
